version: '3'
services:
  tech-app:
    build:
      context: .
    ports:
      - "8080:8080"
    depends_on:
      - mongo_db

  mongo_db:
    image: mongo:latest
    restart: always
    ports:
      - "27017:27017"